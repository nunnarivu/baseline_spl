'''Smoke and golden configs.

These are scaffolding for testing the code, not experiments. They live here so that
`baseline_spl/configs/` lists only the runs worth launching; `baseline_spl/config.py`
falls back to this package, so `BASELINE_CONFIG=<name>` finds them either way.
'''
